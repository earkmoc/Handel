<?php

//Akcja po zapisach:
//select ID from tabele where NAZWA=`dokumentbk`;
//select ID_POZYCJI from tabeles where ID_TABELE=[0] and ID_OSOBY=osoba_id limit 1;
//insert into dokumentz select 0, ID, [0], osoba_id, Now(), ``, 0 from dokum where Find_In_Set(ID,`zaznaczone`)>0

if (!$tabela) {$tabela=$natab;}
require('dokumentz.php');
?>